# Backtest Report: FRED macro z-score momentum (demonstration wiring) (walk-forward)

> Generated by `render_walk_forward_report` (spec `0046-walk-forward`).
> Every figure is computed from held-out periods; the model is refit once
> per fold and never evaluated on data it was fit on.
>
> **Spec:** 0045-fred-point-in-time (panel) + 0046-walk-forward (harness) + 0044-backtesting (engine)
> **Last updated:** 2026-08-21

## Strategy Or Signal

FRED macro z-score momentum (demonstration wiring) — walk-forward over 8-series macro panel: CPIAUCSL, UNRATE, PAYEMS, INDPRO, FEDFUNDS, DGS10, T10Y2Y, GDP, 2000-01-01 to 2026-08-01, monthly.

## Owner

- **Owner:** quant-research

## Summary

Across 5 out-of-sample fold(s) covering 265 held-out period(s): mean fold Sharpe 0.22 (dispersion 0.86), 80.00% of folds positive. Pooled out-of-sample Sharpe 0.28, probabilistic Sharpe 0.907.

## Simulation Contract

- **Out-of-sample construction:** folds come from `make_folds` (spec `0006`) — purged and embargoed, each test block contiguous and later in time than its training set. `fit_predict` is called once per fold on training periods only, and the resulting weights are evaluated on that fold's held-out test periods.
- **Rebalance lag:** 1 period(s), preserved within each fold.
- **Periods per year:** 12
- **Limit of the guarantee:** the harness controls fold construction, refit-per-fold, and held-out evaluation. It cannot establish that `fit_predict` did not close over global data and peek at test periods — that stays `instructions/point_in_time.md`'s concern and the `leakage` gate's.

## Data And Point-In-Time Assumptions

- Source: `gold_fred_point_in_time` in `fred_local.db` (`joshualutkemuller/fred-bronze-to-gold-pipeline`, local mode, no `FRED_API_KEY` in this repository).
- Universe: CPIAUCSL, UNRATE, PAYEMS, INDPRO, FEDFUNDS, DGS10, T10Y2Y, GDP (`GDP` is quarterly and reported stale between releases in this monthly panel by construction -- staleness is visible, not hidden, per `build_panel`'s contract).
- As-of dates: 2000-01-01 to 2026-08-01, month-start, `as_of_snapshot` per date -- a value is used only if its vintage's `realtime_start` was on or before that as-of date.
- Weights come from a demonstration-only cross-sectional z-score of trailing mean returns, fit on each fold's training window and held static across its test window -- **not** a claimed signal; see `specs/0045-fred-point-in-time/spec.md` Non-Goals (weight logic is 0001/0006/0041/0007's job, not this adapter's).

## Costs And Execution

- **Transaction cost:** 5.0 bps per unit turnover.
- **Financing / borrow cost:** 50.0 bps annual on short exposure; this book holds shorts, so borrow is charged.
- **Average turnover per period:** 0.0189
- **Not modelled:** market impact, partial fills, intraday execution — see `execution_optimization.py` (spec `0012`).

## Benchmarks And Baselines

- **Benchmark:** none supplied; a level without a baseline is not an edge.

## Results

| Fold | Test periods | Sharpe | Net return | Max drawdown |
| --- | --- | --- | --- | --- |
| 0 | 53 | 0.69 | 10.14% | 5.16% |
| 1 | 53 | 0.70 | 6.64% | 3.89% |
| 2 | 53 | -1.31 | -12.15% | 13.41% |
| 3 | 53 | 0.61 | 8.39% | 5.10% |
| 4 | 54 | 0.41 | 6.49% | 3.49% |

| Aggregate | Value |
| --- | --- |
| Mean fold Sharpe | 0.22 |
| Sharpe dispersion across folds | 0.86 |
| Folds positive | 80.00% |
| Pooled out-of-sample Sharpe | 0.28 |
| Pooled probabilistic Sharpe | 0.907 |
| Held-out periods evaluated | 265 |

The dispersion and the positive-fold fraction matter as much as the level: a strong mean carried by one fold is not a strategy. The pooled probabilistic Sharpe corrects the pooled figure for sample length, skew, and kurtosis.

## Robustness Tests

- **Out-of-sample:** 5 purged, embargoed walk-forward folds; every figure above is from held-out periods.
- **Multiple testing:** these fold results must **not** be used to select among strategy variants and then reported as out-of-sample — that reintroduces p-hacking through the back door. Selecting across variants requires a deflated Sharpe correction, which this harness deliberately does not provide.
- **Capacity:** see turnover above; no capacity ceiling was modelled.

## Findings

- Mean out-of-sample fold Sharpe 0.22 with dispersion 0.86 across 5 fold(s).
- Costs paid across folds: 5.80%.

## Production Blockers

- Capacity analysis and a benchmark comparison; see `agents/backtest_review/`.

## Decision Recommendation

- Research only. This records out-of-sample behaviour, not a promotion decision.

## Reproducibility

- Deterministic given the same returns, `fit_predict`, and config (`n_folds`=5, `rebalance_lag`=1, `transaction_cost_bps`=5.0).

## Open Questions

- Is the fold count sufficient for the sample length?
