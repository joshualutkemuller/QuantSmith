# Pipeline Manifest: daily_equity_prices

> Generated by `render_pipeline_manifest` (spec `0042-pipeline-builder`).
> Every property below is **declared** by the pipeline's intent and reviewed for completeness -- it is **not verified** against a running pipeline. Idempotency, retry behaviour, and test coverage are claims until `data_pipeline.run` (spec `0011`) exercises them.
>
> **Spec:** 0042-pipeline-builder
> **Last updated:** 2026-08-12

## Ownership

- **Owner / steward:** Data Engineering — market data pod
- **Classification:** internal

## Schedule

- **Cadence / schedule:** daily 06:00 UTC — cron `0 6 * * 1-5` (trading days only)
- **Partitioning:** by trading day (one partition per session)

## Inputs & Outputs

| Direction | Dataset | Source / sink | Data contract |
| --- | --- | --- | --- |
| input | raw_vendor_prices | vendor pricing API (see `sources/`) | `raw_vendor_prices` |
| input | raw_corporate_actions | vendor corporate-actions feed | `raw_corporate_actions` |
| intermediate | adjusted_prices | — | `adjusted_prices` |
| output | warehouse_prices | warehouse table `ANALYTICS.EQUITY_PRICES` | `warehouse_prices` |

## Reliability

- **Retry policy:** 3 attempts per step, transient failures only; contract violations are never retried
- **Backfill:** re-run only partitions missing from the run manifest, 30-day rolling window
- **Idempotency:** (date, security_id) — re-running a partition overwrites by key, never appends

## Observability & Runbook

- **Freshness / SLA:** every step at the current trading day by 07:00 UTC
- **Runbook / on-call:** runbooks/daily_equity_prices.md
- **Escalation:** page the data-eng on-call rota after two consecutive failed partitions
- **Deployment:** promoted through dev → staging → prod by `data_engineering/pipeline_deployment`; not owned by this manifest

## DAG

```text
pull_corporate_actions  [source]  <- (no upstream)
pull_vendor_prices      [source]  <- (no upstream)
adjust_prices           [transform]  <- pull_vendor_prices, pull_corporate_actions
write_warehouse         [sink]  <- adjust_prices

execution order: pull_corporate_actions, pull_vendor_prices, adjust_prices, write_warehouse
```

Steps are listed in execution order, each with its own declared upstream dependencies. Runtime: `src/quantsmith/pipelines/data_pipeline.py` (`0011`); observability: `src/quantsmith/pipelines/pipeline_observability.py` (`0019`).

## Readiness

- **Status:** ready to bind (0 blocking, 0 advisory)

No findings: every checked declaration is present.
