# Backtest Report: Reference long/short worked example

> Generated by `render_backtest_report` (spec `0044-backtesting`) from a
> completed simulation. Every figure below is computed from the realized
> net path, never entered by hand.
>
> **Spec:** 0044-backtesting
> **Last updated:** 2026-08-12

## Strategy Or Signal

Reference long/short worked example — simulated over 3 synthetic assets, 504 daily periods.

## Owner

- **Owner:** Quant Research (SDK reference artifact)

## Summary

Net annualized return -6.80% at 9.58% volatility (Sharpe -0.69, probabilistic Sharpe 0.167). Maximum drawdown 19.76% over 504 periods.

## Simulation Contract

- **Periods simulated:** 504
- **Periods per year:** 252
- **Rebalance lag:** 1 period(s) — weights decided at period `i` are applied to returns at `i + 1`, so a weight vector can never meet a return at or before its own decision index.
- **Limit of that guarantee:** it covers this simulation loop only. It does not establish that the weights supplied were themselves built without look-ahead; that is `instructions/point_in_time.md`'s concern and the `leakage` gate's.

## Data And Point-In-Time Assumptions

- **Synthetic data, disclosed.** Returns are generated from a seeded pseudo-random process (`random.Random(20260812)`), not from a market feed. Per `instructions/data_provenance.md` this is stated rather than implied: no conclusion about any real strategy may be drawn from it.
- The artifact exists to exercise the engine and give the CI-enforced `backtest` gate real content to validate.
- The point-in-time question does not arise for generated data. The real run planned on `gold_fred_point_in_time` uses FRED's `realtime_start` / `realtime_end` vintages so each observation enters as it was known then.

## Costs And Execution

- **Transaction cost:** 5.0 bps per unit turnover, linear in realized turnover.
- **Financing / borrow cost:** 75.0 bps annual on short exposure; this book holds shorts, so borrow is charged.
- **Total costs paid:** 0.55% of starting capital.
- **Not modelled:** market impact, partial fills, and intraday execution. The cost model is linear in turnover and will understate impact for a large book; impact-aware scheduling is `execution_optimization.py` (spec `0012`).

## Benchmarks And Baselines

- **Benchmark:** equal-weight basket of the same assets — mean per-period active return -0.01%.

## Results

| Metric | Value |
| --- | --- |
| Net total return | -13.14% |
| Net annualized return | -6.80% |
| Annualized volatility | 9.58% |
| Sharpe (net) | -0.69 |
| Probabilistic Sharpe (vs. 0) | 0.167 |
| Maximum drawdown | 19.76% |
| Average turnover per period | 0.0020 |
| Hit rate | 46.63% |

The probabilistic Sharpe is the probability the true Sharpe exceeds zero given this sample's length, skew, and kurtosis. A high Sharpe with a low probabilistic Sharpe is a short or fat-tailed sample, not an edge — this is the multiple-testing and p-hacking guard, and it is computed on every run.

## Robustness Tests

- **Out-of-sample:** this report covers a single simulated path. Walk-forward or holdout evaluation over purged, embargoed folds is `return_forecasting.py`'s `make_folds` (spec `0006`); results here are in-sample unless that was applied upstream.
- **Capacity:** see turnover above; no capacity ceiling was modelled.

## Findings

- Net of costs, the strategy returned -6.80% annualized against 9.58% volatility.
- Costs consumed 0.55% of starting capital at an average turnover of 0.0020 per period.

## Production Blockers

- Out-of-sample evidence, capacity analysis, and a benchmark comparison are required before any promotion decision; see `agents/backtest_review/`.

## Decision Recommendation

- Research only. This report records what a supplied weight path would have produced under the stated assumptions; it is not a promotion recommendation.

## Reproducibility

- Deterministic: the same weights, returns, and config (`transaction_cost_bps=5.0`, `borrow_cost_bps_annual=75.0`, `periods_per_year=252`, `rebalance_lag=1`) reproduce this report exactly.

## Open Questions

- Is the cost model adequate for the intended book size?
